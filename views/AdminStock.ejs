<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Stock</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom styles -->
    <style>
        body {
            background-color: #0b132b;
            color: #f8f9fa;
            padding-top: 56px;
        }

        .container {
            background-color: #1c2541;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
            max-width: 800px;
            margin: 20px auto;
            /* Added margin to prevent content from touching the edges */
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
        }

        table {
            width: 100%;
            color: #f8f9fa;
            border: 1px solid #3a506b;
        }

        th,
        td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #3a506b;
        }

        button {
            background-color: #3a506b;
            color: #f8f9fa;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
        }

        button:hover {
            background-color: #5a769a;
        }
    </style>
</head>

<body>
    <%- include(`./partials/navbarLogin.ejs`) %>
        <div class="container">
            <h2>Edit Stock</h2>
            <div>
                <a href="/editStock" class="btn btn-primary">All</a>
                <a href="/editStock/?type=Saham1" class="btn btn-primary">Saham1</a>
                <a href="/editStock/?type=Saham2" class="btn btn-primary">Saham2</a>
                <a href="/editStock/?type=Saham3" class="btn btn-primary">Saham3</a>
            </div>
            <table class="table table-striped table-dark">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Company</th>
                        <th>Type</th>
                        <th>Description</th>
                        <th>Price per Stock</th>
                        <th>Stock</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% stocks.forEach(e=> { %>
                        <tr>
                            <td>
                                <%= e.name %>
                            </td>
                            <td>
                                <%= e.companyName %>
                            </td>
                            <td>
                                <%= e.type %>
                            </td>
                            <td>
                                <%= e.description %>
                            </td>
                            <td>
                                <%= e.price %>
                            </td>
                            <td>
                                <%= e.stock %>
                            </td>
                            <td>
                                <a href="/editStock/<%= e.id %>" class="btn btn-danger btn-sm">Edit</a>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
        <!-- Bootstrap JS Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>