<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Stock</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom styles -->
    <style>
        body {
            background-color: #0b132b;
            color: #f8f9fa;
            padding-top: 56px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #1c2541;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
        }

        label {
            color: #f8f9fa;
        }

        textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            resize: vertical;
            /* Allow vertical resizing */
        }

        button {
            background-color: #3a506b;
            color: white;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #5a769a;
        }
    </style>
</head>

<body>
    <%- include('./partials/navbarLogin.ejs') %>
        <div class="container">
            <br><br><br><br>
            <h2>Edit Stock</h2>
            <form action="" method="post">
                <div class="mb-3">
                    <label for="name" class="form-label">Name:</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= stock.name %>">
                </div>
                <div class="mb-3">
                    <label for="companyName" class="form-label">Company:</label>
                    <input type="text" class="form-control" id="companyName" name="companyName"
                        value="<%= stock.companyName %>">
                </div>
                <div class="mb-3">
                    <label for="type" class="form-label">Type:</label>
                    <select class="form-select" id="type" name="type">
                        <option <%=stock.type==='Saham1' ? 'selected' : '' %>>Saham1</option>
                        <option <%=stock.type==='Saham2' ? 'selected' : '' %>>Saham2</option>
                        <option <%=stock.type==='Saham3' ? 'selected' : '' %>>Saham3</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description:</label>
                    <textarea class="form-control" id="description" name="description"
                        rows="4"><%= stock.description %></textarea>
                </div>
                <div class="mb-3">
                    <label for="pri