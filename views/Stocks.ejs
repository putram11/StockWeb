<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stocks</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #0b132b;
            color: #f8f9fa;
            padding-top: 56px;
            font-family: Arial, sans-serif;
        }

        .container {
            background-color: #1c2541;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
            max-width: 800px;
            margin: 20px auto;
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #f8f9fa;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            color: #f8f9fa;
            border: 1px solid #3a506b;
        }

        th,
        td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #3a506b;
            color: #f8f9fa;
        }

        th {
            background-color: #3a506b;
            color: #f8f9fa;
        }

        tr:nth-child(even) {
            background-color: #253448;
            color: #f8f9fa;

        }

        .btn-stock {
            padding: 5px 10px;
            font-size: 0.875rem;
        }

        .btn-primary {
            background-color: #3a506b;
            border: none;
        }

        .btn-primary:hover {
            background-color: #5a769a;
        }

        .btn-success {
            background-color: #4CAF50;
            border: none;
        }

        .btn-success:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>

    <!-- Navbar inclusion based on session -->
    <% if (!session) { %>
        <%- include(`./partials/navbar.ejs`) %>
            <% } %>
                <% if (session) { %>
                    <%- include(`./partials/navbarLogin.ejs`) %>
                        <% } %>
                            <div class="container">
                                <h2>List Stocks</h2>
                                <div class="mb-3">
                                    <form action="" class="mb-3">
                                        <button type="button" class="btn btn-primary me-2 btn-stock"
                                            onclick="window.location.href='/stocks'">All</button>
                                        <button type="button" class="btn btn-primary me-2 btn-stock"
                                            onclick="window.location.href='/stocks?type=Saham1'">Saham1</button>
                                        <button type="button" class="btn btn-primary me-2 btn-stock"
                                            onclick="window.location.href='/stocks?type=Saham2'">Saham2</button>
                                        <button type="button" class="btn btn-primary me-2 btn-stock"
                                            onclick="window.location.href='/stocks?type=Saham3'">Saham3</button>
                                    </form>
                                </div>
                                <table >
                                    <thead class="table-dark">
                                        <tr>
                                            <th>Name</th>
                                            <th>Company</th>
                                            <th>Type</th>
                                            <th>Description</th>
                                            <th>Price</th>
                                            <th>Stock</th>
                                            <th>Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% stocks.forEach(e=> { %>
                                            <tr>
                                                <td>
                                                    <%= e.name %>
                                                </td>
                                                <td>
                                                    <%= e.companyName %>
                                                </td>
                                                <td>
                                                    <%= e.type %>
                                                </td>
                                                <td>
                                                    <%= e.description %>
                                                </td>
                                                <td>
                                                    <%= e.price %>
                                                </td>
                                                <td>
                                                    <%= e.stock %>
                                                </td>
                                                <td>
                                                    <% if (session) { %>
                                                        <a href="/openStocks/<%= e.id %>"
                                                            class="btn btn-success">Buy</a>
                                                        <% } else { %>
                                                            <a href="/login?msg=Please login first"
                                                                class="btn btn-primary">Buy</a>
                                                            <% } %>
                                                </td>
                                            </tr>
                                            <% }) %>
                                    </tbody>
                                </table>
                            </div>

                            <!-- Bootstrap JS Bundle with Popper -->
                            <script
                                src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>